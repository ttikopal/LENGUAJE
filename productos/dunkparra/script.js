// Función para cambiar la imagen principal
function changeImage(imageSrc) {
    var mainImage = document.getElementById('main-img');  // Obtener la imagen principal
    mainImage.src = imageSrc;  // Cambiar el src de la imagen principal
}